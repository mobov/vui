{
  "name": "@megmore/ui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test":  "lerna run serve --scope lerna-test --stream",
    "init": "lerna init",
    "bootstrap": "lerna bootstrap",
    "vui-guide:start": "lerna run start --scope @megmore/vui-guide --stream",
    "vui:build": "lerna run build --scope @megmore/vui --stream"
  },
  "devDependencies": {
    "lerna": "^3.10.5"
  },
  "workspaces": [
    "packages/*"
  ]
}
