{
  "name": "@megmore/ui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "init": "lerna init",
    "bootstrap": "lerna bootstrap",
    "vui-guide:start": "lerna run serve --scope @megmore/vui-guide --stream",
    "vui:build": "lerna run build --scope @megmore/vui --stream"
  },
  "devDependencies": {
    "lerna": "^3.10.6"
  },
  "workspaces": [
    "packages/*"
  ]
}
