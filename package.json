{
  "name": "@mobov/ui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "init": "lerna init",
    "bootstrap": "lerna bootstrap",
    "vui-guide:start": "lerna run serve --scope @mobov/vui-guide --stream",
    "serve": "vue-cli-service serve",
    "vui:build:lib": "lerna run build:lib --scope @mobov/vui --stream",
    "vui:gen:icons": "lerna run gen:icons --scope @mobov/vui --stream"
  },
  "devDependencies": {
    "lerna": "^3.10.6"
  },
  "workspaces": [
    "packages/*"
  ]
}
