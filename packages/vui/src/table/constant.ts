export enum typeHeader {
  normal = 'normal',
  sticky = 'sticky',
  none = 'none'
}

export enum typeHover {
  none = 'none',
  row = 'row',
  cell = 'cell'
}

export enum typeSelect {
  none = 'none',
  single = 'single',
  multi = 'multi'
}
